#!/bin/sh

# I have two firefox profiles, personal/work
# I _think_ you cannot change profile from within the GUI.
# The 'work' profile is standard, so that's started with plain `firefox`

firefox -P default
