#!/bin/sh

# puts the last 5 items in ~/Downloads, into ripdrag (a drag & drop helper)

if [ $# -eq 0 ]; then
	ls -1t ~/Downloads/* | head -n5 | xargs -d '\n' ripdrag --icon-size 128 --content-height 720
else
	ripdrag --icon-size 128 --content-height 720 "$@"
fi

