% nix

# Search for package
nix search nixpkgs <pkg>

# Apply home-manager
home-manager switch

# Start shell with package installed
nix-shell -p <pkg>

# Update all
nix flake update --flake ~/code/github/chelmertz/dotfiles/nix && home-manager switch

# Remove unused packages (garbage collect, gc)
nix-collect-garbage
