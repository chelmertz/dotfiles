% nix

# Search for package
nix search nixpkgs <pkg>

# Apply home-manager
home-manager switch

# Start shell with package installed
nix-shell -p <pkg>

# Update all (and commit flake.lock)
nix-bump

# Remove unused packages (garbage collect, gc)
nix-collect-garbage
